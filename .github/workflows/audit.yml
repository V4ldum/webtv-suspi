name: Security audit
on:
    schedule:
        - cron: "15 8 1-7 * 0"

jobs:
    audit:
        runs-on: ubuntu-latest
        steps:
            - uses: v4ldum/shared-workflows/.github/workflows/audit-rust.yml@main
